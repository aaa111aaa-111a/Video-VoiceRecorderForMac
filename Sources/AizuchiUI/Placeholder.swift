// Workstream D (AizuchiUI) replaces this file.
// Views must depend only on RecordingControlling + SettingsStore from AizuchiCore.
import AizuchiCore

enum AizuchiUIModule {
    static let contractVersion = 1
}
